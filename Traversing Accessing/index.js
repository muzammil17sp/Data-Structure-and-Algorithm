const array = [10, 20, 30, 40, 50, 60];

// Traversing
for (let i = 0; i < array.length; i++) {
  console.log(array[i]);
}

// Accessing

for (let i = 0; i < array.length; i++) {
  console.log(`${i} index has property of ${array[i]}`);
}
